# CLAUDE.md

Compatibility entrypoint for Claude Code.

`AGENTS.md` is the canonical project context for all AI coding agents. Read
`AGENTS.md` first, then follow the files it links.

Do not duplicate project rules here; keeping one source of truth keeps deenv
model-agnostic during development.
