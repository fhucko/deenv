## .NET build output
bin/
obj/

## TypeScript compiled output (build artifact — never edit by hand; authored as .ts)
DeEnv/Instance/*.js
*.js.map

## Instance runtime data (one per app: <app>-data.json)
*-data.json

## VS / Rider
.vs/
.idea/
*.user

## NuGet
*.nupkg

## Deploy / publish artifacts + transient run logs (build output — never tracked)
artifacts/
*.log
*.tar.gz
kernel-smoke.*
